@using portfolio.Services

<div class="min-h-screen bg-background text-foreground flex flex-col font-mono text-sm">
    <div class="flex-1 flex flex-col">
        <TerminalOutput />

        <!-- Page content rendered inline -->
        <div class="px-4 pb-4">
            @ChildContent
        </div>
    </div>
</div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
